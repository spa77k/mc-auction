# AuctionHouse 合意メモ

Paper 26.1.2 向けのオークションプラグイン。プレイヤー同士が単品アイテムを入札形式で売買できる仕組みを、チェストGUIで提供する。

発案は shuu_9o25 のフィードバック。エンチャント品のように相場が立たない単品や、他者があまり売っていない物は、既存のチェストショップ(QuickShop-Hikari)では価格設定に困る。手数料が高くてもよいので `/irai` のように動くオークションが欲しい、という要望に応える。

## 位置づけ

- 既存の QuickShop-Hikari は置き換えず併存させる。定番品の常設販売はQuickShop、相場が立たない単品はオークション、という住み分け。
- 実装方針・コード構成は同サーバーで稼働中の ContractBoard(`/irai`, `../mc-irai`) を踏襲する。

## 決まったこと

| 項目 | 内容 |
| --- | --- |
| プラグイン名 | AuctionHouse |
| コマンド | `/ah`(エイリアス `/auction`) |
| 対象 | Paper 26.1.2 |
| 落札方式 | 英式入札 + 即決価格の併用。即決価格に達した時点で即落札 |
| 手数料 | 出品料(前払い・返金なし) + 成約手数料。徴収分は消滅し、運営口座等へは送金しない |
| 出品料 | 開始価格の 5%(最低10) |
| 成約手数料 | 落札額の 10% |
| 出品単位 | 手に持ったスタックから個数を指定して1出品 |
| 受け渡し | 保管庫方式。落札品・売れ残りの返却品をプラグインが預かり、本人がGUIから引き出す |
| 入札金 | 入札時に即拘束。上回られたら全額を自動返金 |
| スナイプ対策 | 終了間際の入札で自動延長(回数上限あり) |
| 出品取消 | 入札0件のときだけ可。出品料は返らない |
| 一覧画面 | 並び替え(終了間近 / 新着 / 価格) + タブで自分の出品・入札中・保管庫 |
| 出品期間 | 6 / 12 / 24 / 48時間から選択 |
| 同時出品数 | 1人3件まで |
| 全体告知 | 出品時と落札時にチャットへ流す |

## こちらで決めたこと

- **売上代金は保管庫を経由せず、Vault経済へ直接入金する。** Vaultはオフラインプレイヤーへの入金に対応しており、保管庫に置くと受け取り忘れによる未回収が発生するため。保管庫が預かるのはアイテムのみ。オフライン中に発生した入金・落札・失注は通知テーブルに積み、次回ログイン時にまとめて伝える。
- **ビルド構成**: paper-api `1.21-R0.1-SNAPSHOT`(provided) / `api-version: "1.21"` / `maven.compiler.release` 21。同サーバーで稼働中の SPSMCBuildNoOres 0.2.0 と同じ構成で、Paper 26.1.2 上での実働実績がある。この版の API には `ItemStack#serializeAsBytes()` があり、コンポーネント形式のアイテムを安全に永続化できる。
- **永続化**: SQLite単一ファイル(`plugins/AuctionHouse/auction.db`)。sqlite-jdbc は maven-shade で `net.mcauction.auctionhouse.lib.sqlite` へ relocate する。
- **アイテムの保存形式**: `ItemStack#serializeAsBytes()` の結果を Base64 文字列にしてDBへ格納し、`ItemStack#deserializeBytes()` で復元する。
- **チャット入力の扱い**: 開始価格・即決価格・個数はチャット入力。入力中の発言は全体チャットにもDiscordにも流さない(`AsyncChatEvent` を最優先で受けてキャンセルする)。120秒無操作で自動キャンセル(`input-timeout-seconds`)。
- **最小入札上げ幅**: 現在価格の5%または100の大きい方(config)。
- **自動延長**: 残り3分以内の入札で終了時刻を3分後へ延長、最大10回まで(config)。
- **保管庫は無期限保持。** アイテム消失が最も嫌われるため期限切れ削除はしない。終了済みオークションの履歴レコードのみ30日で削除する(`retention-days`)。
- **権限**: `auction.use`(既定 true)、`auction.admin`(既定 op、不正出品の強制終了と返却用)。
- **出品可能アイテム**: 既定で制限なし(中身入りシュルカーボックスも可)。`config.yml` に禁止マテリアル一覧を用意し、既定は空。
- **メッセージ**: 全て `config.yml` に外出しし、日本語。プレフィックスは `&6[オークション] &r`。

## やらないこと

- 「買います」募集(逆オークション)
- カテゴリによる絞り込み(並び替えのみ)
- 評価・星機能(`/irai` 側の仕組みで、オークションには持ち込まない)
- QuickShop-Hikari との連携・置き換え
- Discord への出品通知連携。本プラグイン自身はDiscordへ送信しない。出品成立時に `AuctionListedEvent` を発火するのみで、実際の通知は購読側の外部プラグイン(EcoLifeAssist等)の役目とする
- サーバー本番反映。ビルドとローカル Paper 26.1.2 での起動確認まで。GitHubリリースと spsmc-infra の Dockerfile への SHA-256 固定は動作確認後に別途行う。
